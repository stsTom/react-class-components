import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/index/$itemId')({
  component: RouteComponent,
})

function RouteComponent() {
  const { itemId } = Route.useParams()

  return <div>Hello, {itemId}!</div>
}
